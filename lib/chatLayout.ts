/** Outer column (matches main content shell in `app/page.tsx`). */
export const CHAT_PAGE_MAX_WIDTH_CLASS = "max-w-6xl";
/** One column for messages + composer (typical agent layout). */
export const CHAT_MAX_WIDTH_CLASS = "max-w-3xl";
/** Narrower column when the evidence viewer is open. */
export const CHAT_SPLIT_MAX_WIDTH_CLASS = "max-w-[44rem]";
/** Horizontal padding aligned across scroll area and dock. */
export const CHAT_GUTTER_X_CLASS = "px-4 sm:px-6";
